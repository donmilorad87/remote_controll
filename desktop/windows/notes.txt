● I can't install packages without sudo. You need to run this yourself:

  sudo apt-get install -y mingw-w64 mingw-w64-tools unzip

  Then the full workflow is:

  cd /home/milner/Desktop/remote/desktop/windows

  # Step 1: Download Windows libraries (run once)
  make -f Makefile.mingw deps

  # Step 2: Build the .exe
  make -f Makefile.mingw

  # Step 3: Package into a dist/ folder ready for Windows
  make -f Makefile.mingw package

  This creates a dist/ folder containing:
  dist/
  ├── remotecontrol-desktop.exe    # The app
  ├── .env                          # Config (edit server IP)
  ├── libcurl-x64.dll              # curl runtime
  └── (other .dlls if needed)

  Copy that dist/ folder to your Windows machine, edit .env with your server IP, and double-click the .exe.

  Want me to wait while you install MinGW, or should I continue with something else?

  ● cd ~/Desktop/remote/desktop/windows
  make -f Makefile.mingw32 clean && make -f Makefile.mingw32 && make -f Makefile.mingw32 package