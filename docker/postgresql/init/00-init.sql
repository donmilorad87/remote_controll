-- Initial database setup
-- Extensions and roles are created here
-- Schema and procedures are in migration files managed by the C server

CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
